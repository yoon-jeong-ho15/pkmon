export default function WalkingAnimation({
  className,
}: {
  className?: string;
}) {
  return (
    <div className={`${className} h-full bg-gray-50 col-span-3`}>
      <div className="w-full">sdf</div>
    </div>
  );
}
